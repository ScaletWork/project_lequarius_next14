import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Home",
};

const Home = () => {
  return (
    <div>
      <h1>Hello World</h1>
    </div>
  );
};

export default Home;
